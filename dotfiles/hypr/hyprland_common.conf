# Set programs that you use
$terminal = uwsm app -- ghostty
$fileManager = uwsm app -- thunar
$menu = fuzzel --launch-prefix="uwsm app -- "
#$menu = rofi -show drun -run-command "uwsm app -- {cmd}" 
$silent_steam = steam -silent

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

#exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = xrandr --output DP-1 --primary
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = uwsm app -s b -- wl-clip-persist --clipboard regular
#exec-once = uwsm app -s b -- sway-audio-idle-inhibit
exec-once = uwsm app -- corectrl
exec-once = uwsm app -- Telegram -startintray
exec-once = uwsm app -- keepassxc
exec-once = uwsm app -- qbittorrent
# -steamos3 gamepade gamescope workaround
#exec-once = steam -silent -steamos3
exec-once = uwsm app -- $silent_steam
#exec-once = uwsm app -- hiddify
exec-once = uwsm app -s b -- wayvnc
#exec-once = hyprctl setcursor Bibata-Modern-Ice 24

# See https://wiki.hyprland.org/Configuring/Environment-variables/
# env = HYPRLAND_LOG_WLR, 1

# env = XDG_SESSION_TYPE, wayland
# env = MOZ_ENABLE_WAYLAND, 1

# env = XCURSOR_SIZE,24
# env = HYPRCURSOR_SIZE,24
# env = XKB_DEFAULT_LAYOUT, us,ru
# env = XKB_DEFAULT_OPTIONS, altwin:menu_win, grp:ctrl_space_toggle

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2 #5
    gaps_out = 15 #20

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgb(f5a97f)
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = false
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true


    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# https://wiki.hyprland.org/Configuring/Dwindle-Layout/
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# https://wiki.hyprland.org/Configuring/Master-Layout/
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#input

input {
    kb_layout = us, ru
    kb_options = altwin:menu_win, grp:ctrl_space_toggle

    follow_mouse = 1
    accel_profile = flat
    force_no_accel = true

    sensitivity = 1 # -1.0 - 1.0, 0 means no modification.

}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = false
}

# https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs
# remove dualshock trackpad as mouse
device {
  name=sony-interactive-entertainment-wireless-controller-touchpad
  enabled=false
}
device {
  name=wireless-controller-touchpad
  enabled=false
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    vfr = true
    force_default_wallpaper = 2 #Enforce any of the 3 default wallpapers. Setting this to 0 or 1 disables the anime background. -1 means “random”. [-1/0/1/2]
    #disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
    disable_hyprland_qtutils_check = true
    middle_click_paste = false
}

experimental {
    xx_color_management_v4 = true
}

# https://wiki.hypr.land/Configuring/Variables/#render
# render {
#     direct_scanout = true
#     cm_fs_passthrough = 2
#     cm_auto_hdr = 2
# }

# https://wiki.hyprland.org/Configuring/Binds/
$mainMod = SUPER # Sets "Windows" key as main modifier

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, F4, killactive
bind = $mainMod SHIFT, F12, exec, de-logout
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating
bind = $mainMod, P, pseudo
bind = $mainMod, J, togglesplit

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Apps
bind = $mainMod, SPACE, exec, $menu
bind = $mainMod, U, fullscreen
bind = $mainMod SHIFT, U, exec, /home/$USER/.config/hypr/monitor_lock.sh
bind = $mainMod SHIFT, M, exec, wayvncctl output-cycle

# PoE
bind = , F5, exec, poe-hotkeys 5
bind = , F4, exec, poe-hotkeys 4
bind = , F1, exec, poe-hotkeys 1

# Etc
bind = $mainMod, Print, exec, screenshotter area
bind = , Print, exec, screenshotter monitor
bind = $mainMod, L, exec, swaylock
bind = $mainMod, backspace, exec, wlogout --protocol layer-shell -b 4 -T 500 -B 500 -L 275

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod SHIFT, mouse:272, movewindow
bindm = $mainMod SHIFT, mouse:273, resizewindow

bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
# bindl = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
#bindl = , XF86AudioMute, exec, wpctl set-volume -p $(hyprctl activewindow -j | jq -r '.pid') 0%
bind = , XF86AudioMute, exec, mute_focus

# Requires playerctl
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
bindl = , XF86AudioNext, exec, playerctl next


# https://wiki.hyprland.org/Configuring/Window-Rules/
# https://wiki.hyprland.org/Configuring/Workspace-Rules/ 

$selector_size = 25% 25%

windowrule = float, title:file_progress
windowrule = float, title:confirm
windowrule = float, title:dialog
windowrule = float, title:download
windowrule = float, title:error
windowrule = float, title:splash
windowrule = float, title:confirmreset
windowrule = float, title:Pick game to add
windowrule = float, title:Extract

windowrule = float, size $selector_size, title:^(Select.+)
windowrule = float, size $selector_size, title:Open File
windowrule = float, size $selector_size, title:Open Folder
windowrule = float, size $selector_size, title:File Upload
windowrule = float, size $selector_size, title:Save As

windowrule = float, title:branchdialog
windowrule = float, class:Lxappearance
windowrule = float, title:^(calcure)$
windowrule = float, title:^(Picture-in-Picture)$
windowrule = float, class:^(qalculate-gtk)$
windowrule = float, class:^(.blueman-manager-wrapped)$
windowrule = float, size 1234,1043, title:^(overskride)$
windowrule = float, title:^(Unlock Database - KeePassXC)$
windowrule = float, class:file-roller
windowrule = float, title:^(Media viewer)$
windowrule = float, title:iwgtk
windowrule = float, title:MangoJuice
windowrule = float, class:imv
windowrule = fullscreen, title:wlogout

windowrule = float, title:^(Volume Control)$
windowrule = size 40% 70%, title:^(Volume Control)$

windowrule = float, class:steam, title:Friends List
windowrule = size 25% 55%, class:steam, title:Friends List
windowrule = center, class:steam, title:Friends List
windowrule = float, $selector_size, title:^(Add Non-Steam Game)

windowrule = tile, initialTitle:^(Path of Building.*)$
windowrule = forcergbx, initialTitle:^(.*Path of Building.*)$ # pob linux fix

windowrule = tile, title:^(Battle.net)$

windowrule = float, size 1500,940, title:^(.*-tui)$

# inhibit
windowrule = idleinhibit focus, class:mpv
windowrule = idleinhibit always, class:^(firefox), fullscreen:1

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# binding workspaces
workspace = 1, monitor:DP-1
workspace = 2, monitor:DP-1
workspace = 3, monitor:DP-1
workspace = 4, monitor:DP-2
workspace = 5, monitor:DP-2
workspace = 6, monitor:DP-2
